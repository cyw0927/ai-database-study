# PostgreSQL connection variable names only
# Do not commit real values or password files.

PGHOST=
PGPORT=
PGDATABASE=
PGUSER=
PGPASSFILE=

# Keep the actual libpq password file outside this repository.
# Unix-like systems require restrictive file permissions.
# On Windows, use the user profile PostgreSQL password-file location or an external protected path.
