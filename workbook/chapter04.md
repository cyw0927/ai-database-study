# Chapter 04 독자 워크북

## 관계형 데이터베이스와 SQL 시작하기

> 이 워크북은 Chapter 04를 읽은 뒤 첫 테이블과 기본 SQL을 직접 실행하고, 예상 결과와 실제 결과를 비교하기 위한 선택형 보조 자료입니다.

## 활동 표시

| 표시 | 의미 |
| --- | --- |
| **핵심 활동** | 첫 테이블과 기본 CRUD를 직접 실행하는 활동 |
| **선택 활동** | PostgreSQL의 편리한 문법과 추가 SQL을 경험하는 활동 |
| **심화 활동** | 데이터 타입, 자동값과 `NULL`의 내부 의미를 확장하는 활동 |

처음 읽는 독자는 핵심 활동만 진행해도 충분합니다.

## 권장 실행 순서

```text
01_create_students.sql
→ 02_insert_students.sql
→ 03_select_students.sql
→ 04_update_delete_students.sql
```

현재 상태 확인은 `verify_students.sql`, 처음부터 다시 시작할 때는 `reset_students.sql`을 사용합니다.

---

# 핵심 활동

## 1. 현재 연결과 실행 범위 확인하기

다음 SQL을 실행합니다.

```sql
SELECT current_database();
SELECT current_user;
SELECT current_schema();
SHOW search_path;
```

| 확인 항목 | 기대 내용 | 실제 결과 |
| --- | --- | --- |
| 현재 데이터베이스 | `ai_database_book` |  |
| 현재 사용자 | 내가 선택한 PostgreSQL 역할 |  |
| 현재 스키마 | 환경에 따라 다를 수 있음 |  |
| `search_path` | 검색 순서를 설명할 수 있음 |  |
| 활성 연결 | 작업하려는 연결 |  |
| Auto-commit | 현재 상태 확인 |  |

다음 문장을 완성합니다.

```text
현재 스키마가 public이 아니어도 본문 예제가 안전한 이유는
________________________________________________________________________
```

```text
변경 SQL을 실행하기 전에 확인할 세 가지는
1.
2.
3.
```

---

## 2. SQL 기본 작성 규칙 확인하기

| 항목 | 작성 예 | 나의 설명 |
| --- | --- | --- |
| SQL 키워드 | `SELECT`, `FROM`, `WHERE` |  |
| 테이블·열 이름 | `public.students`, `created_at` |  |
| 문자열 | `'컴퓨터공학'` |  |
| 숫자 | `3` |  |
| 문장 끝 | `;` |  |
| 한 줄 주석 | `-- 설명` |  |

다음 중 **이 책의 작성 규칙과 열의 타입 의도에 맞는 표현**을 표시합니다. PostgreSQL은 일부 문자열 리터럴을 문맥에 맞게 변환할 수 있지만, 초급 실습에서는 숫자 열에는 숫자 리터럴을, 문자열 열에는 작은따옴표로 감싼 문자열 리터럴을 명확하게 사용합니다.

```sql
WHERE grade = '3'
WHERE grade = 3
WHERE major = 컴퓨터공학
WHERE major = '컴퓨터공학'
```

작은따옴표와 큰따옴표의 차이를 적습니다.

```text
'김민지':
"name":
```

---

## 3. CREATE TABLE 구조 읽기

```sql
CREATE TABLE public.students (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    major VARCHAR(100),
    grade INTEGER,
    created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

| 열 | 데이터 타입 | 제약조건·기본값 | 역할 |
| --- | --- | --- | --- |
| `id` |  |  |  |
| `name` |  |  |  |
| `email` |  |  |  |
| `major` |  |  |  |
| `grade` |  |  |  |
| `created_at` |  |  |  |

확인 질문:

1. 행을 고유하게 구분하는 열은 무엇인가요?
2. `NULL`로 저장할 수 없는 열은 무엇인가요?
3. 중복을 허용하지 않는 열은 무엇인가요?
4. 값을 생략하면 `NULL`이 될 수 있는 열은 무엇인가요?
5. 값을 생략해도 IDENTITY나 DEFAULT로 자동값이 채워질 수 있는 열은 무엇인가요?
6. `NOT NULL`인데도 INSERT에서 직접 값을 생략할 수 있는 열은 무엇이며, 그 이유는 무엇인가요?

`01_create_students.sql` 실행 결과:

```text
테이블 생성 성공 여부:
DBeaver에서 확인한 위치:
발생한 오류:
```

---

## 4. 데이터 타입 선택하기

다음 값에 적절한 타입을 연결합니다.

| 값 | 적절한 타입 | 이유 |
| --- | --- | --- |
| 학생 수 | `INTEGER` / `TEXT` |  |
| 가격 12500.50 | `NUMERIC` / `FLOAT` |  |
| 이름 | `VARCHAR` / `INTEGER` |  |
| 활성 여부 | `BOOLEAN` / `DATE` |  |
| 생년월일 | `DATE` / `TEXT` |  |
| 등록 시각 | `TIMESTAMPTZ` / `INTEGER` |  |

금액에 `NUMERIC`을 우선 검토하는 이유:

```text
________________________________________________________________________
```

---

## 5. INSERT 전후 비교하기

### 단일 행

```sql
INSERT INTO public.students (name, email, major, grade)
VALUES ('김민지', 'minji@example.com', '컴퓨터공학', 2)
RETURNING id, name, created_at;
```

| 항목 | 예상 | 실제 결과 |
| --- | --- | --- |
| 추가 행 수 |  |  |
| 직접 입력한 열 |  |  |
| 자동값 열 |  |  |
| 반환 열 |  |  |
| 실제 생성된 `id` | 미리 단정하지 않음 |  |

### 여러 행과 선택값

`02_insert_students.sql`을 실행한 뒤 기록합니다.

| 항목 | 기대 상태 | 실제 상태 |
| --- | --- | --- |
| 전체 학생 수 | 6 |  |
| 이준호 학년 | 3 |  |
| 박서연 | 존재 |  |
| 컴퓨터공학 학생 수 | 2 |  |
| 전공·학년이 `NULL`인 학생 | 윤서진 |  |

```sql
SELECT COUNT(*) AS student_count
FROM public.students;
```

가장 큰 `id`와 학생 수가 항상 같지 않을 수 있는 이유:

```text
________________________________________________________________________
```

---

## 6. SELECT와 WHERE 결과 예상하기

다음 SQL을 실행하기 전에 반환 열, 예상 행 수와 순서를 적습니다.

```sql
SELECT id, name, grade
FROM public.students
WHERE grade >= 3
ORDER BY grade DESC, id ASC;
```

| 항목 | 나의 예상 | 실제 결과 |
| --- | --- | --- |
| 반환 열 |  |  |
| 예상 학생 |  |  |
| 예상 행 수 |  |  |
| 정렬 순서 |  |  |

다음 조건을 SQL로 작성합니다.

```text
전공이 컴퓨터공학인 학생
학년이 2 이하인 학생
전공이 컴퓨터공학 또는 데이터사이언스인 학생
컴퓨터공학 전공이면서 학년이 3 이상인 학생
```

---

## 7. LIKE와 NULL 확인하기

### 문자열 패턴

| 요구사항 | SQL 조건 |
| --- | --- |
| 이름이 김으로 시작 |  |
| 이름에 민 포함 |  |
| 이름이 우로 끝남 |  |
| 김 뒤에 정확히 두 문자 |  |

### NULL

다음 문장을 완성합니다.

```text
NULL은 0이나 빈 문자열이 아니라
________________________________________________________________________
```

다음 SQL의 결과를 예상합니다.

```sql
SELECT *
FROM public.students
WHERE major = NULL;
```

```text
예상 결과:
이유:
```

올바른 조회를 작성합니다.

```sql
-- 전공이 NULL인 학생

-- 전공이 입력된 학생
```

`major <> '경영학'`에서 윤서진이 제외되는 이유:

```text
________________________________________________________________________
```

---

## 8. DISTINCT, ORDER BY와 LIMIT 사용하기

### DISTINCT

전공을 중복 없이 조회하는 SQL을 작성합니다.

```sql

```

`DISTINCT`가 원본 테이블을 변경하지 않는 이유:

```text
________________________________________________________________________
```

### ORDER BY와 LIMIT

`created_at` 기준 상위 3행을 동률까지 안정적인 순서로 조회합니다.

```sql
SELECT *
FROM public.students
ORDER BY ______________________________
LIMIT 3;
```

보조 정렬 기준이 필요한 이유:

```text
________________________________________________________________________
```

---

## 9. UPDATE를 안전하게 실행하기

이준호의 학년을 4로 수정합니다.

| 단계 | SQL 또는 확인 결과 |
| --- | --- |
| 수정 전 SELECT |  |
| 예상 영향 행 수 |  |
| UPDATE 조건 |  |
| RETURNING 결과 |  |
| 실제 영향 행 수 |  |
| 수정 후 SELECT |  |

실행 흐름:

```text
SELECT
→ UPDATE RETURNING
→ 영향받은 행 수 확인
→ SELECT
```

`WHERE`가 없는 다음 SQL의 영향 범위를 설명합니다.

```sql
UPDATE public.students
SET grade = 1;
```

```text
________________________________________________________________________
```

수정 실습 후 상태:

| 항목 | 실제 상태 |
| --- | --- |
| 학생 수 |  |
| 이준호 학년 |  |
| 박서연 |  |

---

## 10. DELETE를 안전하게 실행하기

박서연 학생을 삭제합니다.

| 단계 | SQL 또는 확인 결과 |
| --- | --- |
| 삭제 전 SELECT |  |
| 예상 영향 행 수 |  |
| DELETE 조건 |  |
| RETURNING 결과 |  |
| 실제 영향 행 수 |  |
| 삭제 후 SELECT |  |

`WHERE`가 없는 다음 SQL의 영향 범위를 설명합니다.

```sql
DELETE FROM public.students;
```

```text
________________________________________________________________________
```

삭제 실습 후 상태:

| 항목 | 실제 상태 |
| --- | --- |
| 학생 수 |  |
| 이준호 학년 |  |
| 박서연 |  |

---

## 11. AI가 만든 SQL 검토하기

AI가 다음 SQL을 제안했다고 가정합니다.

```sql
DELETE FROM public.students
WHERE major = '컴퓨터공학';
```

실행하지 않고 검토합니다.

| 검토 질문 | 나의 판단 |
| --- | --- |
| 현재 연결과 테이블이 맞는가? |  |
| 사용하는 열과 타입이 맞는가? |  |
| `WHERE` 조건과 `NULL` 처리가 맞는가? |  |
| 예상 영향 행 수는 몇 개인가? |  |
| 먼저 실행할 `SELECT`는 무엇인가? |  |
| 실제 요구사항과 일치하는가? |  |

검토용 `SELECT`:

```sql

```

최종 판단:

```text
[ ] 실행 가능
[ ] 수정 필요
[ ] 실행하지 않음

이유:
```

---

# 선택 활동

## 12. PostgreSQL 편의 기능 경험하기

### RETURNING

`INSERT`, `UPDATE`, `DELETE`에서 `RETURNING`을 사용했을 때의 장점을 적습니다.

```text
________________________________________________________________________
```

### ILIKE

```sql
SELECT *
FROM public.students
WHERE email ILIKE '%EXAMPLE.COM'
ORDER BY id ASC;
```

`LIKE`와 다른 점:

```text
________________________________________________________________________
```

### NULLS LAST

```sql
SELECT *
FROM public.students
ORDER BY grade DESC NULLS LAST, id ASC;
```

`NULLS LAST`의 역할:

```text
________________________________________________________________________
```

### 여러 열 UPDATE

다음 SQL의 대상과 변경 열을 설명합니다. 기본 실습에서는 실행하지 않습니다.

```sql
UPDATE public.students
SET major = '소프트웨어공학',
    grade = 3
WHERE email = 'minji@example.com';
```

---

## 13. 형 변환과 추가 타입

다음 SQL의 결과를 기록합니다.

```sql
SELECT CAST('3' AS INTEGER) AS cast_result;
SELECT '3'::INTEGER AS shorthand_result;
```

| 표현 | 결과 | 특징 |
| --- | --- | --- |
| `CAST()` |  | 표준적 표현 |
| `::` |  | PostgreSQL 축약 문법 |

`UUID`와 `JSONB`를 사용할 수 있는 사례를 하나씩 적습니다.

```text
UUID:
JSONB:
```

---

# 심화 활동

## 14. 자동값과 NULL의 내부 의미 확장하기

다음 문장이 맞는지 판단합니다.

| 문장 | 맞음 / 틀림 | 이유 |
| --- | --- | --- |
| 첫 학생의 `id`는 항상 1이다. |  |  |
| `id`는 현재 학생 수와 같다. |  |  |
| 삭제된 `id`는 반드시 재사용된다. |  |  |
| 같은 트랜잭션에서 입력한 여러 행의 `created_at`이 같을 수 있다. |  |  |
| `id`가 더 크면 항상 실제 생성 시각도 더 늦다고 단정할 수 있다. |  |  |
| `NULL = NULL`은 항상 참이다. |  |  |

`IS DISTINCT FROM`을 사용한 조건을 작성합니다.

```sql

```

---

## 15. 초기 상태 복원과 최종 점검

조회 문제를 다시 풀려면 초기 데이터 상태를 복원합니다.

```text
reset_students.sql
→ 01_create_students.sql
→ 02_insert_students.sql
→ verify_students.sql
```

최종 확인:

| 항목 | 확인 |
| --- | --- |
| 현재 DB와 사용자 확인 |  |
| 테이블 구조 설명 가능 |  |
| 초기 학생 6명 입력 |  |
| `WHERE`와 논리 조건 작성 |  |
| `NULL` 조회 가능 |  |
| `DISTINCT`, 정렬과 `LIMIT` 사용 |  |
| 안전한 수정·삭제 절차 설명 |  |
| AI SQL 영향 범위 검토 |  |

## 권장 해설 요약

- CRUD Create는 `INSERT`, 테이블 구조 생성은 `CREATE TABLE`이다.
- 문자열은 작은따옴표로 감싸고 숫자는 따옴표 없이 작성한다.
- `NULL`은 0이나 빈 문자열이 아니며 `IS NULL`로 확인한다.
- `DISTINCT`는 조회 결과의 중복을 제거하고 원본 데이터를 바꾸지 않는다.
- `ORDER BY`가 없으면 결과 순서는 보장되지 않는다.
- `LIMIT`에는 안정적인 정렬 기준을 함께 사용한다.
- `UPDATE`와 `DELETE` 전에는 같은 조건으로 `SELECT`한다.
- `NOT NULL`은 최종 저장값이 `NULL`일 수 없다는 뜻이며, DEFAULT가 있으면 INSERT에서 열을 생략할 수 있다.
- 자동 생성 `id`는 학생 수·학번·실제 생성 시각의 대체값이 아니다.
- 같은 트랜잭션에서는 `CURRENT_TIMESTAMP`가 같은 값일 수 있으므로 정렬의 동률 기준을 따로 둔다.
- AI SQL은 예상 반환 행 또는 영향받는 행 수를 먼저 확인한다.
